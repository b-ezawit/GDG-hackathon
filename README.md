## Getting Started

1. **Clone the repo**
2. **Install dependencies:** `npm install`
3. **Set up Environment:** Add `GROQ_API_KEY` to `.env.local` (or your deployment secrets) for live Groq calls.
4. **Run the app:** `npm run dev`
5. **Open [http://localhost:3000](http://localhost:3000)**
